const fs=require('fs'),path=require('path');
const source=fs.readFileSync(path.join(process.cwd(),'src/data/legacy/network-data.ts'),'utf8');
const match=source.match(/export const networkData = (\[[\s\S]*\]) as const satisfies/); if(!match)throw Error('networkData missing'); const rows=JSON.parse(match[1]);
const q=v=>`'${String(v).replace(/'/g,"''")}'`;
const values=rows.map((row,index)=>`(${index+1}, ${q(`legacy_html:${index+1}:${row.name}`)}, ${q(JSON.stringify(row))}::jsonb)`).join(',\n');
const cte=`with source_rows(source_row, source_key, payload) as (values\n${values}\n)`;
let sql=`-- 0009: deterministic legacy HTML network import. Generated by scripts/generate-legacy-network-import.cjs.\ncreate table if not exists buzzerhood.legacy_network_imports (source_key text primary key, source_row integer not null unique, payload jsonb not null, partner_id uuid references buzzerhood.partners(id) on delete restrict, imported_at timestamptz not null default now());\nalter table buzzerhood.legacy_network_imports enable row level security; alter table buzzerhood.legacy_network_imports force row level security;\ndrop policy if exists legacy_network_imports_admin_read on buzzerhood.legacy_network_imports; create policy legacy_network_imports_admin_read on buzzerhood.legacy_network_imports for select to authenticated using (buzzerhood.has_permission('partners.manage')); grant select on buzzerhood.legacy_network_imports to authenticated;\n\n`;
sql+=`${cte}\ninsert into buzzerhood.partners (display_name, partner_type, tier, category, niche, verification_status, is_public, source_data) select payload->>'name', payload->>'partner_type', payload->>'tier', payload->>'category', payload->>'niche', 'unclaimed', true, jsonb_build_object('source','legacy_html','source_row',source_row,'payload',payload) from source_rows on conflict ((source_data->>'source'), (source_data->>'source_row')) where source_data->>'source' = 'legacy_html' do update set display_name=excluded.display_name,partner_type=excluded.partner_type,tier=excluded.tier,category=excluded.category,niche=excluded.niche,source_data=excluded.source_data,updated_at=now();\n\n`;
sql+=`${cte}\ninsert into buzzerhood.legacy_network_imports(source_key,source_row,payload,partner_id) select s.source_key,s.source_row,s.payload,p.id from source_rows s join buzzerhood.partners p on p.source_data->>'source'='legacy_html' and (p.source_data->>'source_row')::integer=s.source_row on conflict(source_key) do update set payload=excluded.payload,partner_id=excluded.partner_id;\n\n`;
sql+=`insert into buzzerhood.partner_platform_accounts(partner_id,platform,handle,is_primary) select partner_id,case when payload->>'platform_category'='Lainnya' then 'Other' else payload->>'platform_category' end,case when payload->>'platform_category'='TikTok' then nullif(payload->>'tt','-') else nullif(payload->>'ig','-') end,true from buzzerhood.legacy_network_imports on conflict(partner_id,platform,handle) do update set is_primary=true,updated_at=now();\n\n`;
sql+=`insert into buzzerhood.partner_platform_accounts(partner_id,platform,handle,is_primary) select partner_id,'Instagram',nullif(payload->>'ig','-'),false from buzzerhood.legacy_network_imports where nullif(payload->>'ig','-') is not null and payload->>'platform_category'<>'Instagram' on conflict(partner_id,platform,handle) do nothing;\ninsert into buzzerhood.partner_platform_accounts(partner_id,platform,handle,is_primary) select partner_id,'TikTok',nullif(payload->>'tt','-'),false from buzzerhood.legacy_network_imports where nullif(payload->>'tt','-') is not null and payload->>'platform_category'<>'TikTok' on conflict(partner_id,platform,handle) do nothing;\n\n`;
sql+=`insert into buzzerhood.partner_audience_metrics(platform_account_id,metric_type,metric_value,observed_at,metric_source,raw_value) select a.id,case when lower(i.payload->>'metric_label') like '%pengunjung%' then 'monthly_visitors'::buzzerhood.metric_type when lower(i.payload->>'metric_label') like '%subscriber%' then 'subscribers'::buzzerhood.metric_type when lower(i.payload->>'metric_label') like '%anggota%' then 'members'::buzzerhood.metric_type when lower(i.payload->>'metric_label') like '%view%' then 'views'::buzzerhood.metric_type else 'followers'::buzzerhood.metric_type end,coalesce((i.payload->>'metric_value')::numeric,0),'2026-09-02T00:00:00Z','legacy_html',jsonb_build_object('source','legacy_html','source_row',i.source_row,'metric_label',i.payload->>'metric_label','total',i.payload->>'total') from buzzerhood.legacy_network_imports i join buzzerhood.partner_platform_accounts a on a.partner_id=i.partner_id and a.is_primary=true where not exists(select 1 from buzzerhood.partner_audience_metrics m where m.platform_account_id=a.id and m.metric_source='legacy_html' and m.raw_value->>'source_row'=i.source_row::text);\n\ninsert into buzzerhood.schema_migrations(version,filename) values('0009','0009_legacy_network_import.sql') on conflict(version) do nothing;\n`;
fs.writeFileSync('database/migrations/0009_legacy_network_import.sql',sql); console.log(rows.length);
