import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { createClientOrganization } from '@/features/onboarding/onboarding-api';
const slugify=(v:string)=>v.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
export function ClientOnboardingPage(){const nav=useNavigate();const [name,setName]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage('');try{await createClientOrganization(name,slugify(name));nav('/client');}catch(error){setMessage(error instanceof Error?error.message:'Organisasi gagal dibuat.');}finally{setBusy(false)}}return <main className="workspace-page"><p className="eyebrow">CLIENT ONBOARDING</p><h1>Buat organisasi client</h1><form className="auth-card" onSubmit={submit}><label>Nama organisasi<input required minLength={2} maxLength={160} value={name} onChange={e=>setName(e.target.value)}/></label><button className="btn-solid" disabled={busy}>{busy?'Membuat…':'Buat organisasi'}</button>{message?<p className="form-message">{message}</p>:null}</form></main>}
